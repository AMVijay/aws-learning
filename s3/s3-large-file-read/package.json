{
  "name": "s3-large-file-read",
  "version": "1.0.0",
  "description": "## Project Set Commands * npm init *",
  "main": "index.js",
  "scripts": {
    "webpack-build": "npx rimraf ./dist && npx mkdirp ./dist && npx webpack && zip -rq lambda.zip dist",
    "build": "npx rimraf ./dist && npx mkdirp ./dist && tsc && zip -rq lambda.zip node_modules dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "VIJAYaraaghavan Manoharan",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "mkdirp": "^2.1.6",
    "rimraf": "^4.4.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.301.0"
  }
}
